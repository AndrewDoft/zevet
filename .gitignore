node_modules/
.env
*.key
*.log
data/
.DS_Store
desktop/node_modules/
desktop/out/
desktop/build/icon.png

# Local install artifacts: install.mjs writes these into whatever repo you wire,
# including this one. They carry absolute paths from one machine.
.claude/settings.json
# Claude Code puts an agent's isolated git worktree here. A worktree inside the
# repo it is a worktree OF is committed as a gitlink the moment `git add -A`
# runs, which is exactly what happened on 2026-09-21 — the 0.2.26 commit
# carried a submodule pointer to a scratch branch.
.claude/worktrees/
*.bak.*

# The editor bundle's build tree. `editor/` is the only place in this repository
# with dependencies, and none of them ship: `npm run build` in editor/ turns
# them into hub/public/editor.js, and THAT is what is committed and served.
#
# Note what is deliberately NOT ignored: hub/public/editor.js and
# hub/public/editor.js.map. The hub has no build step — it deploys by
# `git pull && docker restart` and serves hub/public verbatim — so a bundle
# that is not in git is a bundle that does not exist in production. The map is
# committed too, because an 800 KiB minified stack trace pasted from a
# teammate's console is otherwise unreadable.
editor/node_modules/

# The hub's account store. NEVER commit this and never let it into a
# release tarball: /srv/zevet is extracted over the top of itself, so a
# tracked var/accounts.json would overwrite the live sessions and the master
# secret on every deploy. See hub/accounts.mjs.
#
# ⚠️ IT IS THE REPO ROOT, NOT hub/. `defaultAccountsFile` resolves
# hub/../var, because the tarball's root IS /srv/zevet on the box. This said
# hub/var/ first, and a stray accounts.json -- with a master secret in it --
# turned up staged for commit.
/var/

# Per-repo agent wiring and release artifacts: local state, never committed.
.opencode/plugins/zevet.js
zevet-*.tar.gz

# Playwright MCP scratch: page snapshots and screenshots taken while working.
.playwright-mcp/

# Scratch output that lands in the repo root while working: the gate writes
# gate-out.txt, sprite-test.md is a rendering scratch. Neither is a source file,
# and release-check.mjs refuses to publish from a tree that is not clean.
gate-out.txt
sprite-test.md
